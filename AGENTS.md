# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Remo") is currently a blank scaffold with only a `README.md`. There are no application services, dependencies, build systems, or test frameworks configured yet.

- **No services to run**: The repo has no runnable application code.
- **No dependencies**: No `package.json`, `requirements.txt`, `pyproject.toml`, or other dependency manifests exist.
- **No lint/test/build**: No linting, testing, or build tooling is configured.

Once application code is added, update this section with:
- How to install dependencies and which package manager to use.
- How to run, lint, test, and build the application.
- Any non-obvious startup caveats or environment requirements.
